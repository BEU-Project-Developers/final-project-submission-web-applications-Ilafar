@model List<PricingService>
<!DOCTYPE html>
<html>
<head>
    <style>
        table {
            font-family: arial, sans-serif;
            border-collapse: collapse;
            width: 100%;
        }

        td, th {
            border: 1px solid #dddddd;
            text-align: left;
            padding: 8px;
        }

        tr:nth-child(even) {
            background-color: #dddddd;
        }
    </style>
</head>
<body>

    <h2>PricingServices list :</h2>
    <div>
        <a class="btn btn-primary" asp-action="create" asp-controller="pricingservices">
            Add new pricingservice
        </a>
    </div>
    <table>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Crud</th>
        </tr>
        @foreach (var pricingservice in Model)
        {
            <tr>
                <td>@pricingservice.Pricing.Id</td>
                <td>@pricingservice.Pricing.Name</td>
                <td>
                    <a class="btn btn-primary" asp-action="edit" asp-controller="pricingservices" asp-route-id="@pricingservice.Pricing.Id">
                        Edit
                    </a>
                    <a class="btn btn-primary" asp-action="detail" asp-controller="pricingservices" asp-route-id="@pricingservice.Pricing.Id">
                        Details
                    </a>
                    <a asp-action="delete" asp-controller="pricingservices" asp-route-id="@pricingservice.Pricing.Id" class="btn btn-danger">
                        Delete
                    </a>
                </td>
            </tr>
        }


    </table>

</body>
</html>

